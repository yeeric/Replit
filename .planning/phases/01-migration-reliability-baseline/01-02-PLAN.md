---
phase: 01-migration-reliability-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - php/migrate.php
  - start.sh
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "Startup exits non-zero when any critical migration/seed step fails."
    - "Sponsor seed/type path is backed by a canonical PostgreSQL enum and matching table column type."
    - "Migration output clearly separates WARN and FATAL outcomes with a final structured summary."
    - "Running migration repeatedly converges to deterministic seeded state."
  artifacts:
    - path: "php/migrate.php"
      provides: "Critical/non-critical step orchestration, structured logs, enum-safe schema path, deterministic reseed flow"
    - path: "start.sh"
      provides: "Synchronous migrate-then-serve startup gate with fail-fast behavior"
  key_links:
    - from: "start.sh"
      to: "php/migrate.php"
      via: "startup migration command exit status"
      pattern: "php php/migrate.php"
    - from: "php/migrate.php"
      to: "sponsor.sponsorlevel"
      via: "enum type creation and typed column DDL"
      pattern: "sponsor_level"
    - from: "php/migrate.php"
      to: "final migration summary"
      via: "severity-classified step logging"
      pattern: "WARN|FATAL|summary"
---

<objective>
Implement deterministic, observable migration/startup behavior that satisfies all Phase 1 data reliability requirements.

Purpose: Deliver fail-fast startup safety, canonical sponsor enum typing, clear fatal/warn observability, and repeat-run seed determinism.
Output: Hardened `php/migrate.php` and `start.sh` verified by the Wave 1 CLI harness.
</objective>

<execution_context>
@/Users/ericxye/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ericxye/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-migration-reliability-baseline/01-CONTEXT.md
@.planning/phases/01-migration-reliability-baseline/01-RESEARCH.md
@.planning/phases/01-migration-reliability-baseline/01-VALIDATION.md
@.planning/phases/01-migration-reliability-baseline/01-01-PLAN.md
@php/migrate.php
@start.sh

<interfaces>
From `php/migrate.php`:
```php
function safeExec(PDO $db, string $sql, string $label): bool
```

From `start.sh`:
```bash
php php/migrate.php
exec php -S 0.0.0.0:5000 php/index.php
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor migration runner to explicit criticality + deterministic reseed</name>
  <files>php/migrate.php</files>
  <read_first>php/migrate.php, .planning/phases/01-migration-reliability-baseline/01-CONTEXT.md, .planning/phases/01-migration-reliability-baseline/01-RESEARCH.md, tests/phase1/fatal-path-check.sh, tests/phase1/enum-path-check.sh, tests/phase1/log-classification-check.sh, tests/phase1/repeat-run-idempotency.sh</read_first>
  <behavior>
    - Test 1: A forced failure in a critical step produces `FATAL` output with step identifier and exits with code `1` (D-01, D-02, D-03).
    - Test 2: Non-critical failures are logged as `WARN`, counted separately, and do not stop startup when no critical failure occurred (D-07, D-08).
    - Test 3: Sponsor schema and seed path uses PostgreSQL enum `sponsor_level` for column type and insert casts/values (D-04).
    - Test 4: Two consecutive migration runs produce identical seeded-state counts/fingerprint values after full-table reset + reseed (D-05, D-06).
  </behavior>
  <action>Replace continue-on-error `safeExec` flow with an explicit ordered step registry containing stable `step_id` values and `critical` boolean metadata per D-02. Implement structured step logging (`INFO`, `WARN`, `FATAL`) and final summary fields with `critical_failures`, `warnings`, and total `steps` per D-07/D-08/D-09. Ensure critical failures terminate process with `exit(1)` and include step id + concise reason + exit code per D-01/D-03. Implement canonical enum path per D-04 by creating `sponsor_level` type if missing, defining `sponsor.sponsorlevel sponsor_level NOT NULL`, and aligning sponsor seed values to that type. Replace attendee-count guard with full delete-and-reseed strategy across related tables in dependency-safe order inside a transaction per D-05/D-06, preserving deterministic fixture set on every run per D-10.</action>
  <verify>
    <automated>bash tests/phase1/enum-path-check.sh && bash tests/phase1/log-classification-check.sh && bash tests/phase1/repeat-run-idempotency.sh</automated>
  </verify>
  <acceptance_criteria>`rg -n "step_id|critical|WARN|FATAL|critical_failures|warnings" php/migrate.php` returns structured classification fields; `rg -n "CREATE TYPE sponsor_level|sponsorlevel sponsor_level" php/migrate.php` returns enum creation and typed column definition; `rg -n "BEGIN|COMMIT|ROLLBACK|TRUNCATE TABLE" php/migrate.php` returns transactional reseed flow.</acceptance_criteria>
  <done>Migration script enforces locked failure/type/reseed policies and passes enum, logging, and idempotency integration checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce synchronous startup gating on migration outcome</name>
  <files>start.sh</files>
  <read_first>start.sh, php/migrate.php, .planning/phases/01-migration-reliability-baseline/01-CONTEXT.md, tests/phase1/fatal-path-check.sh</read_first>
  <behavior>
    - Test 1: If `php php/migrate.php` exits non-zero, `start.sh` exits non-zero and does not run `php -S`.
    - Test 2: If migration exits zero, `start.sh` proceeds to server start command.
  </behavior>
  <action>Update `start.sh` to strict synchronous startup sequencing per D-10/D-11/D-12: add `set -euo pipefail`, run `php php/migrate.php` before serving, and keep `exec php -S 0.0.0.0:5000 php/index.php` only on successful migration exit. Emit explicit startup log lines that identify migration phase and serve phase so fatal-path scripts can assert gating behavior.</action>
  <verify>
    <automated>bash tests/phase1/fatal-path-check.sh</automated>
  </verify>
  <acceptance_criteria>`rg -n "set -euo pipefail" start.sh` returns one line; `rg -n "php php/migrate.php" start.sh` returns migration-first invocation; `rg -n "exec php -S 0.0.0.0:5000 php/index.php" start.sh` returns serve command after migration command in file order.</acceptance_criteria>
  <done>Startup script blocks server launch on critical migration failures and preserves migrate-then-serve ordering.</done>
</task>

</tasks>

<verification>
Run:
1. `bash tests/phase1/quick-migration-check.sh`
2. `bash tests/phase1/full-migration-reliability.sh`
</verification>

<success_criteria>
- DATA-01: Critical migration failure causes non-zero startup path.
- DATA-02: Sponsor type/seed path is enum-consistent and valid.
- DATA-03: Logs and summary clearly separate WARN and FATAL outcomes.
- DATA-04: Repeated migration runs converge to identical seeded state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-migration-reliability-baseline/01-02-SUMMARY.md`
</output>
